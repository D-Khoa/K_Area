namespace BaseLibrary.Global
{
    public enum GameState { Play , Pause, Stop}
    public enum Difficult { Easy, Normal, Hard }
    public enum CharacterType { Empty, Enemy, Ally }
    public enum ElementDirection { Up , Down, Left, Right}
}
